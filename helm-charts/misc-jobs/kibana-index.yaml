### Convert this to a job
### Write the helm chart to deploy jobs

apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    kompose.cmd: kompose convert
    kompose.version: 1.22.0 (955b78124)
  creationTimestamp: null
  labels:
    io.kompose.service: kibana-sidecar
  name: kibana-sidecar
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: sidecar
  strategy: {}
  template:
    metadata:
      annotations:
        kompose.cmd: kompose convert
        kompose.version: 1.22.0 (955b78124)
      creationTimestamp: null
      labels:
        io.kompose.service: sidecar
    spec:
      containers:
        - args:
            - sh
            - -c
            - './wait_for_service.sh http://kibana.default.svc.cluster.local:5601; ./create_index_pattern.sh '
          env:
            - name: INDEX_PATTERN
              value: heartbeat*
            - name: KIBANA_HOST
              value: http://kibana.default.svc.cluster.local:5601
          image: sidecar
          name: sidecar
          resources: {}
      restartPolicy: Always
status: {}
